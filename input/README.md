# Input directory

`synthetic_rhp_fixture.docx` is a fabricated demonstration fixture, not the assignment's RHP. It was generated locally because no source attachment was available. Replace it with the real RHP (without overwriting it elsewhere) when supplied, then create a new ground-truth annotation set before reporting source-document metrics.
